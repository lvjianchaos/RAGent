# 项目经历

## Ragent — 企业级 RAG 智能体平台

**项目介绍**：基于 Java 17 + SpringBoot 3 构建的企业级 Agentic RAG 平台，覆盖文档入库、意图识别、多路检索、智能问答完整链路。系统实现意图定向检索与全局向量检索的双路召回策略，配合问题重写、会话记忆压缩、术语归一化等机制提供高准确率问答服务；采用分布式队列限流控制模型调用并发，通过三态熔断器、流式首包探测与优先级降级链保障多模型场景下的高可用性；支持 MCP 协议集成外部工具调用，扩展智能体能力边界。

**技术架构**：SpringBoot + MyBatis-Plus + PostgreSQL + Milvus（可切换 pgvector）+ Redis + Redisson + Apache Tika + Sa-Token + TransmittableThreadLocal

**职责描述**：

- **多路并行检索引擎**：设计 SearchChannel 策略接口，实现意图定向检索与全局向量检索双路召回，基于 CompletableFuture 并行调度各检索通道，并依据意图置信度动态启停通道（置信度不足时全局通道自动兜底）；召回结果经过去重、版本过滤、分数归一化、Rerank 重排序的后处理器责任链逐级精炼，解决单一检索方式覆盖率不足的问题，兼顾召回率与精准度。

- **分布式队列限流**：基于 Redis 信号量 + ZSET 有序队列 + Pub/Sub 实现分布式排队限流，通过 Lua 脚本保证入队与队头窗口判断的原子性，信号量许可支持租约自动过期防止死锁，跨实例 Pub/Sub 广播唤醒并本地合并通知避免惊群效应；支持公平排队与超时拒绝，配合 SSE 实时推送排队状态，有效控制高并发场景下的模型调用压力。

- **多模型容错与高可用**：自研三态熔断器（CLOSED / OPEN / HALF_OPEN）按模型独立维护健康状态，连续失败达到阈值自动熔断，冷却期后半开放行单探测请求，成功恢复、失败续熔；配合优先级降级链在多家模型供应商候选间自动切换，并通过装饰器模式实现流式首包探测，首包超时即无缝切换至下一候选模型，用户端无感知，保障多模型场景下的高可用性。

- **线程池治理与全链路追踪**：按负载特征拆分 8 个专用线程池（对话入口、多路检索、内部检索、意图分类、记忆摘要、上下文组装、MCP 批量调用、模型流式输出），队列类型与拒绝策略各异，避免线程池资源竞争与任务阻塞；通过 TransmittableThreadLocal + TtlExecutors 包装线程池，实现用户上下文与 Trace 上下文在异步线程间透传，配合 AOP + 注解实现全链路调用追踪（traceId、耗时、输入输出、异常信息），保障异步场景下追踪链路的完整性。

- **问题重写与会话记忆管理**：基于 LLM 实现问题重写与拆分，支持多轮对话上下文补全、复杂问题拆解为多个子问题并行检索，配合术语归一化与规则兜底策略，改写失败自动回退原始问题，缓解用户口语化表达与知识库标准术语不匹配的问题；采用滑动窗口保留最近 N 轮对话，超出阈值由 LLM 自动生成摘要并持久化至 PostgreSQL，在控制 Token 成本的同时保留关键上下文，避免长对话场景下的 Token 爆炸。

- **树形意图识别体系**：实现基于 LLM 的树形意图分类器，意图树缓存于 Redis，支持 KB（知识检索）、MCP（工具调用）、SYSTEM（系统指令）三类意图；意图数量较多时按 Domain 拆分并行分类，识别置信度不足时主动引导用户澄清，为意图定向检索与 MCP 工具路由提供决策依据，提升意图识别准确性与用户体验。

- **可编排文档入库流水线**：实现节点编排的可视化文档入库流水线（数据获取、文档解析、文本分块、内容增强、向量索引），节点配置持久化至数据库，支持条件跳转与环检测，任务与节点级独立执行日志可精确定位故障环节；基于 Apache Tika 实现 PDF / Word / Excel / Markdown 等多格式文档解析，提供固定大小与结构感知等多种分块策略，保留上下文完整性并提升 Chunk 质量。

- **MCP 工具调用与可插拔架构**：集成 MCP 协议实现外部工具调用，通过意图识别关联目标工具，由 LLM 自动提取调用参数，支持多工具并行执行，实现知识检索与业务系统调用的无缝融合；落地策略、工厂、注册表、模板方法、装饰器、责任链等设计模式，检索通道、后处理器、入库节点、MCP 工具均可插拔替换，新增组件实现接口并注册为 Spring Bean 即可被自动发现，零配置生效。

---

## 附：使用说明（投递前请删除本节）

**内容核实依据**（均来自项目真实代码与配置，面试可放心展开）：

| 职责点 | 代码 / 配置依据 |
| :--- | :--- |
| 双路召回与置信度兜底 | `SearchChannel`、`VectorGlobalSearchChannel`（意图置信度低于阈值或未识别意图时全局通道自动启用）、`rag.search.channels.*` 配置 |
| 后处理器责任链 | `SearchResultPostProcessor`（去重 / 版本过滤 / 分数归一化 / Rerank，按 order 串联） |
| 队列限流 | `rag.rate-limit.global`（max-concurrent、max-wait-seconds、lease-seconds）、`@ChatRateLimit` AOP |
| 三态熔断器 | `ModelHealthStore`（CLOSED / OPEN / HALF_OPEN，失败阈值与冷却时长可配置：`ai.selection.*`） |
| 首包探测 | `ProbeBufferingCallback`（装饰器模式增强流式回调） |
| 8 个专用线程池 | 对话入口 / 多路检索 / 内部检索 / 意图分类 / 记忆摘要 / 上下文组装 / MCP 批量调用 / 模型流式输出 |
| 问题重写 | `QueryRewriteService`（rewriteWithSplit 支持历史与多问句拆分，失败回退原问题） |
| 会话记忆 | `ConversationMemoryService`、`rag.memory.*`（history-keep-turns、summary-start-turns、summary-max-chars） |
| 意图识别 | `IntentClassifier`（支持串行 / 按 Domain 并行两种实现策略） |
| 入库流水线 | `IngestionNode`（模板方法统一执行流程）、`rag.knowledge.schedule` 定时扫描 |
| 多模型路由 | `ai.chat.candidates`（priority 优先级降级链，覆盖百炼 / Ollama / SiliconFlow 等多家供应商） |

**使用建议**：

1. 职责描述共 8 条，篇幅紧张时可优先保留前 5 条（检索、限流、熔断、线程池、重写记忆为含金量最高的核心亮点），后 3 条按目标岗位 JD 取舍。
2. 可根据目标行业调整项目业务场景（如企业知识库、智能客服等），技术描述无需变动。
3. 面试追问高频点建议提前准备：Lua 脚本如何保证原子性、ZSET 队头窗口判断逻辑、熔断器半开状态并发控制（`halfOpenInFlight` 单探测）、首包探测的装饰器实现细节、子问题并行检索的结果合并策略。
